{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": [
        "^prisma:db:migrate",
        "^prisma:db:seed",
        "^prisma:generate"
      ],
      "outputs": ["dist/**"]
    },
    "audio-engine:build": {
      "dependsOn": ["^build"],
      "outputs": ["build/**"]
    },
    "web:build": {
      "dependsOn": ["audio-engine:build"],
      "env": ["VERCEL_URL", "PORT", "NODE_ENV"],
      "outputs": [".next/**"]
    },
    "lint": {
      "outputs": []
    },
    "dev": {
      "dependsOn": ["prisma:db:seed"],
      "cache": false,
      "persistent": true
    },
    "db:start": {
      "cache": false,
      "persistent": true
    },
    "prisma:db:migrate": {
      "dependsOn": ["db:start"],
      "cache": false
    },
    "prisma:db:seed": {
      "dependsOn": ["prisma:db:migrate"],
      "cache": false
    },
    "prisma:generate": {
      "cache": false
    }
  }
}
